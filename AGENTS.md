# Before considering task done

1. if changes required clean build run `docker compose run --rm clean`
2. run `docker compose run --rm build`, if there are any errors, fix them.
3. update readme to reflect your changes
