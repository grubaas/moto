[package]
name = "moto"
version = "0.1.0"
edition = "2021"
description = "Bare-metal Rust firmware for Teensy 4.1 (i.MX RT1062 / Cortex-M7), simulated in Renode"

[dependencies]
cortex-m = "0.7"
cortex-m-rt = "0.7"
panic-halt = "0.2"

[profile.release]
opt-level = "s"
debug = 2
lto = true
